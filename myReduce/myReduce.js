/**
 * re-implements `Array.prototype.reduce`
 *
 * e.g.
 * myReduce([1, 2, 3], (acc, current) => acc + current, 0);
 *
 * should return 0 + 1 + 2 + 3 = 6
 *
 */
export default function myReduce(arr, fn, seed) {}
